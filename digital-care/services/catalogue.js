function normalizeProduct(input={}){const name=String(input.name||'').trim();const sku=String(input.sku||'').trim();const category=String(input.category||'').trim();const price=Number(input.priceUgx);const stock=Math.max(0,Math.floor(Number(input.stockQuantity)||0));if(!name||!sku||!category||!Number.isFinite(price)||price<0)throw new Error('Name, SKU, category and valid UGX price are required.');return {name,sku,slug:String(input.slug||name).toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,''),category,description:String(input.description||'').slice(0,5000),priceUgx:price,stockQuantity:stock,requiresPrescription:Boolean(input.requiresPrescription),imageUrl:input.imageUrl?String(input.imageUrl):null};}
module.exports={normalizeProduct};
