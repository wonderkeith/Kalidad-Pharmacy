import { db, auth } from "./firebase-config.js";
import { collection, addDoc, getDocs, query, where, orderBy, serverTimestamp } from "https://www.gstatic.com/firebasejs/12.18.0/firebase-firestore.js";
export async function createOrder({items,deliveryAddress,customerNotes="",paymentMethod="cash_on_delivery",deliveryFee=0}){if(!auth.currentUser)throw new Error("Please log in before placing an order.");const subtotal=items.reduce((n,x)=>n+Number(x.price)*Number(x.quantity),0),total=subtotal+Number(deliveryFee);const ref=await addDoc(collection(db,"orders"),{orderNumber:"",userId:auth.currentUser.uid,customer:{name:auth.currentUser.displayName||auth.currentUser.email,phone:deliveryAddress.phone||""},items,subtotal,deliveryFee:Number(deliveryFee),total,paymentMethod,paymentStatus:"pending",orderStatus:"pending",deliveryAddress,customerNotes,createdAt:serverTimestamp(),updatedAt:serverTimestamp()});return ref.id}
export async function getMyOrders(){if(!auth.currentUser)throw new Error("Not signed in");const s=await getDocs(query(collection(db,"orders"),where("userId","==",auth.currentUser.uid),orderBy("createdAt","desc")));return s.docs.map(d=>({id:d.id,...d.data()}))}
