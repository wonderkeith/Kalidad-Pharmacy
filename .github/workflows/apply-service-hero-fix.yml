name: Service hero repair

on:
  push:
    branches:
      - fix/service-hero-treatment

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check workflow
        run: |
          echo "Workflow parsed and job started"
          node --version
          python --version
