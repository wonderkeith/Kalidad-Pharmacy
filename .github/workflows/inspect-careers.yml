name: Inspect Careers Hero

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Show hero selectors
        shell: bash
        run: |
          grep -n -i -E "hero|careers" careers.html | head -80
